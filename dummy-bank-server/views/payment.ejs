<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dummy Bank - Confirm Payment</title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 600px; margin: 50px auto; text-align: center; }
        h1 { color: #004d99; }
        .amount { font-size: 1.5em; font-weight: bold; color: #2e7d32; }
        form { margin: 20px 0; }
        button { padding: 12px 24px; margin: 10px; font-size: 1.1em; cursor: pointer; }
        .approve { background: #2e7d32; color: white; border: none; }
        .decline { background: #d32f2f; color: white; border: none; }
    </style>
</head>
<body>
    <h1><%= provider %> Bank - Confirm Payment</h1>
    <p>PayX wants to debit <span class="amount">₹<%= amount  %></span> from your account.</p>
    <p>User ID: <%= userId %></p>

    <p>Please confirm or decline the payment</p>
    
    <form action="/success/<%= token %>" method="POST" style="display:inline;">
        <button type="submit" class="approve">Approve Payment</button>
    </form>
    
    <form action="/failure/<%= token %>" method="POST" style="display:inline;">
        <button type="submit" class="decline">Decline Payment</button>
    </form>

    <p><a href=" http://localhost:5173/addtowallet">Back to PayTM</a></p>
   
</body>
</html>